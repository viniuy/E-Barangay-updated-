'use client';
import { ServiceDirectory } from '@/components/user/services/UserServiceDirectory';

export default function ServicesPage() {
  return <ServiceDirectory onNavigate={() => {}} onSelectService={() => {}} />;
}
